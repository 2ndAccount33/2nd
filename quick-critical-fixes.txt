🚨 CRITICAL FIXES NEEDED - Copy & Paste This

Your site looks dull because of ONE MAJOR BUG and a few refinements needed:

═══════════════════════════════════════════════════════════════════

## 🔴 CRITICAL BUG: Lines Moving WRONG Direction!

**Problem:** Lines move UP/DOWN instead of LEFT/RIGHT across screen

**Fix in index.css (line 83-101):**

REPLACE:
```css
@keyframes moveLine {
  0% { transform: translateY(-100%); opacity: 0; }
  5% { opacity: 1; }
  95% { opacity: 1; }
  100% { transform: translateY(100%); opacity: 0; }
}
```

WITH:
```css
@keyframes moveLine {
  0% { transform: translateX(-300px); opacity: 0; }
  5% { opacity: 1; }
  95% { opacity: 1; }
  100% { transform: translateX(calc(100vw + 300px)); opacity: 0; }
}
```

This makes lines sweep HORIZONTALLY like spotlights!

═══════════════════════════════════════════════════════════════════

## 🔧 FIX 2: Remove Duplicate Lines

**In SetuLandingPage.jsx (lines 284-287), DELETE:**
```jsx
{/* Animated Lines - Multiple Instances */}
<div className="animated-line" style={{ left: '15%' }}></div>
<div className="animated-line" style={{ left: '45%', animationDelay: '4s' }}></div>
<div className="animated-line" style={{ left: '75%', animationDelay: '8s' }}></div>
```

Keep ONLY the second set (lines 289-294) in the fixed container.

═══════════════════════════════════════════════════════════════════

## 🎨 FIX 3: Lighten Cards (Optional but Recommended)

**In index.css (lines 52-59), REPLACE:**
```css
.glass-card {
  @apply bg-black/40 backdrop-blur-xl border border-white/[0.08] rounded-2xl;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.glass-card-strong {
  @apply bg-white/[0.07] backdrop-blur-xl border border-white/10 rounded-2xl;
}
```

WITH:
```css
.glass-card {
  @apply bg-black/30 backdrop-blur-xl border border-white/[0.05] rounded-2xl;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
}

.glass-card-strong {
  @apply bg-white/[0.04] backdrop-blur-xl border border-white/[0.06] rounded-2xl;
}
```

═══════════════════════════════════════════════════════════════════

## 🔍 FIX 4: Ensure Cursor Hidden

**In index.css (around line 8-12), ensure this exists:**
```css
* {
  cursor: none;
}
```

═══════════════════════════════════════════════════════════════════

## ✅ VERIFICATION

After changes, you should see:
✅ Lines moving ACROSS screen (left to right) like spotlights
✅ Only 3-4 lines (not 6)
✅ Lighter, more refined card appearance
✅ Site feels dynamic, not static

The main issue is **Fix #1** - the animation direction bug. This single change will transform your site from "dull" to "dynamic"!

═══════════════════════════════════════════════════════════════════

PRIORITY ORDER:
1. Fix animation direction (translateY → translateX)
2. Remove duplicate lines
3. Lighten cards (optional)
4. Verify cursor

The translateX fix is CRITICAL - do this first!
